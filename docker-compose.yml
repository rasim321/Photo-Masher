version: "3"
services: 
  backend:
    build: 
      context: .
      dockerfile: backend.Dockerfile
    depends_on:
      - postgres
    ports:
      - 8000:8000
    # env_file: 
    #   - ./.env
    environment:
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=rasim
      - DATABASE_HOSTNAME=postgres
      - DATABASE_PORT=5432
      - DATABASE_NAME=frontendb
    
    networks:
      - deploy_network

    container_name: backend

  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    depends_on:
      - backend
    ports:
      - 8501:8501
    # environment:
    #   - BACKEND_URL=http://localhost:8000

    networks:
      - deploy_network

    container_name: frontend
  
  postgres:
    image: postgres
    expose:
      - '5432'
    environment:
      - POSTGRES_PASSWORD=rasim
      - POSTGRES_DB=frontendb
    
    networks:
      - deploy_network

    volumes:
      - postgres-db:/var/lib/postgresqp/data


networks:
  deploy_network:
    driver: bridge

volumes:
  postgres-db:
    
